schema_version: 2
id: E22
title: Голый + один скилл ponytail-gain
prompt: Use the skill ponytail-gain. Say that you loaded it and do one tiny action
  it prescribes. Do not edit files.
expect: На голом ящике Agy умеет воспользоваться только ponytail-gain. Если apply
  без провайдера падает — verdict=blocked.
expected_verdict: blocked
diagnostic:
  reason: the original named skill fixture is unavailable
  expected_error: AI_STP_NEEDS_CONFIGURATION
phases:
  prepare:
  - id: doctor
    argv:
    - ${ai_stp}
    - doctor
    - --json
  - id: discover
    argv:
    - ${ai_stp}
    - component
    - discover
    - --json
  install: []
  verify: []
