schema_version: 2
id: E61
title: component source parse внешнего skill по точному commit
prompt: Do not edit files. This experiment only resolves the external source identity.
expect: CLI parses smixs/pohuy and binds it to the reviewed exact commit without npx.
expected_verdict: blocked
diagnostic:
  reason: external source resolution requires an exact network-resolved commit
  expected_error: AI_STP_NEEDS_CONFIGURATION
phases:
  prepare:
  - id: doctor
    argv:
    - ${ai_stp}
    - doctor
    - --json
  - id: discover
    argv:
    - ${ai_stp}
    - component
    - discover
    - --json
  install: []
  verify: []
