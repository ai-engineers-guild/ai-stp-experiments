{
  "mcpServers": {
    "ai-stp-e32-local": {
      "command": "py",
      "args": [
        "-3",
        "-u",
        "-c",
        "import json,sys; tools=[{'name':'ai_stp_e32_probe','description':'Return the unique E32 experiment marker','inputSchema':{'type':'object','properties':{}}}];\nfor line in sys.stdin:\n r=json.loads(line); m=r.get('method'); out={'jsonrpc':'2.0','id':r.get('id')}; out.update({'result':{'protocolVersion':'2024-11-05','capabilities':{'tools':{}},'serverInfo':{'name':'ai-stp-e32-local','version':'1.0'}}} if m=='initialize' else {'result':{'tools':tools}} if m=='tools/list' else {'result':{'content':[{'type':'text','text':'AI_STP_MCP_E32_OK'}]}} if m=='tools/call' else {'result':{}}); print(json.dumps(out),flush=True)"
      ]
    }
  }
}
