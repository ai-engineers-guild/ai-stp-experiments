schema_version: 2
id: E59
title: target status после попытки установки
prompt: Do not edit files. List skills (whatever is actually there).
expect: status совпадает с диском и с тем, что назвал Agy.
expected_verdict: blocked
diagnostic:
  reason: target status requires a preceding installation in the same run
  expected_error: AI_STP_NEEDS_CONFIGURATION
phases:
  prepare:
  - id: doctor
    argv:
    - ${ai_stp}
    - doctor
    - --json
  - id: discover
    argv:
    - ${ai_stp}
    - component
    - discover
    - --json
  install: []
  verify: []
