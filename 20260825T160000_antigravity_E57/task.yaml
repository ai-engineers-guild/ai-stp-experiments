schema_version: 2
id: E57
title: import inspect обезличенного снимка с credential reference
prompt: Do not edit files. This is an inspect-only experiment.
expect: inspect видит MCP, сообщает имя redacted credential-поля и не пишет в target.
expected_verdict: blocked
diagnostic:
  reason: the retained live snapshot contains credentials and is unsafe to inspect
  expected_error: AI_STP_NEEDS_CONFIGURATION
phases:
  prepare:
  - id: doctor
    argv:
    - ${ai_stp}
    - doctor
    - --json
  - id: discover
    argv:
    - ${ai_stp}
    - component
    - discover
    - --json
  install: []
  verify: []
