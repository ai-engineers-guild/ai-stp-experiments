antigravity:
  cli: agy
  harness_id: antigravity
  target: ${user_home}/.gemini
  isolate_home: true
  provider: antigravity-setup-system
  delegate: ${experiments}/delegate.py
  delegate_backend: ${skills_root}/antigravity-delegate/scripts/delegate_antigravity.py

codex:
  cli: codex
  harness_id: codex
  target: ${user_home}/.codex
  isolate_home: true
  provider: codex-setup-system
  delegate: ${experiments}/delegate.py
  delegate_backend: ${skills_root}/codex-delegate/scripts/delegate_codex.py

claude-code:
  cli: claude
  harness_id: claude-code
  target: ${user_home}/.claude
  isolate_home: true
  provider: claude-code-setup-system
  delegate: ${experiments}/delegate.py
  delegate_backend: ${skills_root}/claude-code-delegate/scripts/delegate_claude.py

pi:
  cli: pi
  harness_id: pi
  target: ${user_home}/.pi/agent
  isolate_home: true
  provider: pi-setup-system
  delegate: ${experiments}/delegate.py
  delegate_backend: ${skills_root}/pi-delegate/scripts/delegate_pi.py
  runtime: pi

pi-omp:
  cli: omp
  harness_id: pi
  target: ${user_home}/.omp/agent
  isolate_home: true
  provider: pi-setup-system
  delegate: ${experiments}/delegate.py
  delegate_backend: ${skills_root}/pi-delegate/scripts/delegate_pi.py
  runtime: omp

opencode:
  cli: opencode
  harness_id: opencode
  target: ${user_home}/.config/opencode
  isolate_home: true
  provider: opencode-setup-system
  delegate: ${experiments}/delegate.py
  delegate_backend: ${skills_root}/opencode-delegate/scripts/delegate_opencode.py

grok-build:
  cli: grok
  harness_id: grok-build
  target: ${user_home}/.grok
  isolate_home: true
  provider: grok-build-setup-system
  delegate: ${experiments}/delegate.py
  delegate_backend: ${skills_root}/grok-build-delegate/scripts/delegate_grok.py
