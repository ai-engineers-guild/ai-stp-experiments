schema_version: 2
id: E33
title: Голый + один MCP context7
prompt: Use the context7 MCP. Resolve library id for react. Reply with the id only.
  Do not edit files.
expect: Agy вызывает context7. Других MCP нет. Скиллов нет.
expected_verdict: blocked
diagnostic:
  reason: the named external MCP needs its own public fixture, network policy, or
    credentials
  expected_error: AI_STP_NEEDS_CONFIGURATION
phases:
  prepare:
  - id: doctor
    argv:
    - ${ai_stp}
    - doctor
    - --json
  - id: discover
    argv:
    - ${ai_stp}
    - component
    - discover
    - --json
  install: []
  verify: []
