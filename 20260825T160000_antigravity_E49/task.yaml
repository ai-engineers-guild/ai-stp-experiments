schema_version: 2
id: E49
title: Голый + только agent
prompt: List agents or subagents you can delegate to. Do not edit files.
expect: Либо видит агента, либо gaps no agents — записать.
observer_failure_verdict: blocked
fixtures:
- source: ${experiments}/fixtures/agent-local/config/agents/experiment-reviewer.md
  destination: .gemini/config/agents/experiment-reviewer.md
phases:
  prepare:
  - id: doctor
    argv:
    - ${ai_stp}
    - doctor
    - --json
  - id: discover
    argv:
    - ${ai_stp}
    - component
    - discover
    - --json
  - id: adopt
    argv:
    - ${ai_stp}
    - component
    - adopt
    - --path
    - ${user_home}/.gemini/config/agents/experiment-reviewer.md
    - --json
    capture:
      component_id: data.stable_id
      component_revision: data.revision_id
  - id: developer
    argv:
    - ${ai_stp}
    - passport
    - developer
    - init
    - --json
  - id: device
    argv:
    - ${ai_stp}
    - passport
    - device
    - refresh
    - --json
  - id: project
    argv:
    - ${ai_stp}
    - project
    - passport
    - --root
    - ${project}
    - --json
  - id: update
    argv:
    - ${ai_stp}
    - component
    - passport
    - update
    - --id
    - ${component_id}
    - --expected-revision
    - ${component_revision}
    - --from
    - ${experiments}/fixtures/agent-local/passport-patch.json
    - --confirm
    - --json
    capture:
      component_revision: data.revision_id
  - id: release
    argv:
    - ${ai_stp}
    - component
    - version
    - release
    - --id
    - ${component_id}
    - --json
  - id: propose
    argv:
    - ${ai_stp}
    - select
    - propose
    - --harness
    - ${harness_id}
    - --project
    - ${project}
    - --member
    - ${component_id}@1.0
    - --json
    capture:
      proposal_id: data.proposals.0.proposal_id
  - id: confirm
    argv:
    - ${ai_stp}
    - select
    - confirm
    - --proposal
    - ${proposal_id}
    - --confirm
    - --json
    capture:
      setup_id: data.stable_id
  install:
  - id: plan
    argv:
    - ${ai_stp}
    - install
    - plan
    - --setup
    - ${setup_id}@1.0
    - --project
    - ${project}
    - --harness
    - ${harness_id}
    - --provider
    - ${provider}
    - --unverified-provider
    - --protocol-version
    - '3'
    - --target
    - ${target}
    - --json
    capture:
      operation_id: data.operation_id
      plan_digest: data.plan_digest
  - id: approve
    argv:
    - ${ai_stp}
    - install
    - approve
    - --operation
    - ${operation_id}
    - --plan-digest
    - ${plan_digest}
    - --json
  - id: apply
    argv:
    - ${ai_stp}
    - install
    - apply
    - --operation
    - ${operation_id}
    - --provider
    - ${provider}
    - --json
    expect_json:
      ok: true
      data.state: verified
  verify:
  - id: observer
    argv:
    - ${python}
    - ${delegate}
    - --harness
    - ${harness_id}
    - --backend
    - ${delegate_backend}
    - --cwd
    - ${project}
    - --target
    - ${target}
    - --timeout
    - 3m
    - --output-dir
    - ${run_root}\observer
    - --task
    - List agents or subagents you can delegate to. Do not edit files.
    expect_exit: 0
    expect_json:
      exit_code: 0
      raw.status: SUCCESS
    expect_json_contains:
      response: experiment-reviewer
expect_paths:
- path: ${target}/config/agents/experiment-reviewer.md
  contains: experiment-reviewer
