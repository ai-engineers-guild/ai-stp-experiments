schema_version: 2
id: E02
title: Поставить канонический скилл ai-stp
prompt: Use the ai-stp skill. Run or describe the first command it tells you (doctor).
  Do not edit files.
expect: Agy видит ai-stp и опирается на doctor/help. Других пользовательских скиллов
  нет.
expected_verdict: blocked
diagnostic:
  reason: canonical ai-stp skill lifecycle is separate from setup components
  expected_error: AI_STP_NEEDS_CONFIGURATION
phases:
  prepare:
  - id: doctor
    argv:
    - ${ai_stp}
    - doctor
    - --json
  - id: discover
    argv:
    - ${ai_stp}
    - component
    - discover
    - --json
  install: []
  verify: []
