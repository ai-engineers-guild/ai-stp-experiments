schema_version: 2
id: E56
title: Пустой сетап (явная пустота)
prompt: List skills, MCP, hooks. Expect empty. Do not edit files.
expect: Пусто. Agy не падает.
expected_verdict: blocked
diagnostic:
  reason: ai-stp has no explicit empty-setup authoring command
  expected_error: AI_STP_NEEDS_CONFIGURATION
phases:
  prepare:
  - id: doctor
    argv:
    - ${ai_stp}
    - doctor
    - --json
  - id: discover
    argv:
    - ${ai_stp}
    - component
    - discover
    - --json
  install: []
  verify: []
