schema_version: 2
id: E35
title: Голый + один MCP eslint
prompt: Use the eslint MCP. List the tools you actually have from it. Call one read-only
  tool if safe. Do not edit files.
expect: Agy вызывает eslint. Других MCP нет. Скиллов нет.
expected_verdict: blocked
diagnostic:
  reason: the named external MCP needs its own public fixture, network policy, or
    credentials
  expected_error: AI_STP_NEEDS_CONFIGURATION
phases:
  prepare:
  - id: doctor
    argv:
    - ${ai_stp}
    - doctor
    - --json
  - id: discover
    argv:
    - ${ai_stp}
    - component
    - discover
    - --json
  install: []
  verify: []
