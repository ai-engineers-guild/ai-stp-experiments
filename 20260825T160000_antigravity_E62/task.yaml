schema_version: 2
id: E62
title: Восстановить полный дом бэкапом ai-stp
prompt: List skills, MCP, hooks. Compare to the E00 baseline names. Do not edit files.
expect: Эталон E00 вернулся. Если CLI не восстанавливает — blocked, не руками npx.
expected_verdict: blocked
diagnostic:
  reason: live restore is disabled until a credential-free baseline exists
  expected_error: AI_STP_NEEDS_CONFIGURATION
phases:
  prepare:
  - id: doctor
    argv:
    - ${ai_stp}
    - doctor
    - --json
  - id: discover
    argv:
    - ${ai_stp}
    - component
    - discover
    - --json
  install: []
  verify: []
