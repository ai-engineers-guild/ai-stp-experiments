schema_version: 2
id: E54
title: Сетап только MCP (все 14)
prompt: List MCP servers by name. Call context7 resolve-library-id for react. Do not
  edit files.
expect: 14 MCP, 0 скиллов. context7 работает.
expected_verdict: blocked
diagnostic:
  reason: the fourteen named MCP servers require network or credential policy
  expected_error: AI_STP_NEEDS_CONFIGURATION
phases:
  prepare:
  - id: doctor
    argv:
    - ${ai_stp}
    - doctor
    - --json
  - id: discover
    argv:
    - ${ai_stp}
    - component
    - discover
    - --json
  install: []
  verify: []
