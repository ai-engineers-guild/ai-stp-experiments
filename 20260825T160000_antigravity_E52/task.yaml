schema_version: 2
id: E52
title: 'Два скилла в одном сетапе: ponytail + spec-write '
prompt: First use ponytail (one short sentence). Then use spec-write (three headings).
  Do not edit files.
expect: Оба умения сработали. Третьих скиллов нет.
expected_verdict: blocked
diagnostic:
  reason: the original skills need distinct released component fixtures
  expected_error: AI_STP_NEEDS_CONFIGURATION
phases:
  prepare:
  - id: doctor
    argv:
    - ${ai_stp}
    - doctor
    - --json
  - id: discover
    argv:
    - ${ai_stp}
    - component
    - discover
    - --json
  install: []
  verify: []
