schema_version: 2
id: E26
title: Голый + один скилл spec-review
prompt: Use spec-review. Name three defects a spec must not have. Do not edit files.
expect: На голом ящике Agy умеет воспользоваться только spec-review. Если apply без
  провайдера падает — verdict=blocked.
expected_verdict: blocked
diagnostic:
  reason: the original named skill fixture is unavailable
  expected_error: AI_STP_NEEDS_CONFIGURATION
phases:
  prepare:
  - id: doctor
    argv:
    - ${ai_stp}
    - doctor
    - --json
  - id: discover
    argv:
    - ${ai_stp}
    - component
    - discover
    - --json
  install: []
  verify: []
