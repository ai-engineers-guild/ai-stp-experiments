schema_version: 1
id: C05
component_type: command
scenario: read-only
fixtures:
- main
variants:
- antigravity
- pi
- grok-build
observe:
  expect:
    marker: AI_STP_C05
  probe: Invoke the installed native prompt command `/experiment-c05` and report
    its exact result. Do not edit files.
  pi_probe:
    mode: exact_input
    input: /experiment-c05
