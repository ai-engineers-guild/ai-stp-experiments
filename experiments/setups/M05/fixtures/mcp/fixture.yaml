schema_version: 1
objects:
- M05-mcp-a
- M05-mcp-b
states:
  baseline:
    paths:
    - path: config/mcp_config.json
      exists: false
  installed:
    paths:
    - path: config/mcp_config.json
      exists: true
  restored:
    same_as: baseline
fixture_type: component
variants:
  antigravity:
    payload: payload/common
    managed_paths:
    - config/mcp_config.json
  grok-build:
    payload: payload/common
    managed_paths:
    - .mcp.json
  pi:
    payload: payload/harnesses/pi
    managed_paths:
    - extensions/m05-mcps/package.json
    - extensions/m05-mcps/index.ts
    - extensions/m05-mcps/.mcp.json
