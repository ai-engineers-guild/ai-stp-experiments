schema_version: 1
objects:
- M03-mcp-a
- M03-mcp-b
states:
  baseline:
    paths:
    - path: config/mcp_config.json
      exists: false
  installed:
    paths:
    - path: config/mcp_config.json
      exists: true
  restored:
    same_as: baseline
fixture_type: component
variants:
  antigravity:
    payload: payload/harnesses/antigravity
    managed_paths:
    - config/mcp_config.json
  grok-build:
    payload: payload/harnesses/grok-build
    managed_paths:
    - config.toml
  pi:
    payload: payload/harnesses/pi
    managed_paths:
    - extensions/m03-mcps/package.json
    - extensions/m03-mcps/index.ts
    - extensions/m03-mcps/.mcp.json
  codex:
    payload: payload/harnesses/codex
    exclude:
    - .mcp.json
    managed_paths:
    - config.toml
    source_subpath: config.toml
    authoring_path: .codex/config.toml
