schema_version: 1
objects:
- M03-skill-a
states:
  baseline:
    paths:
    - path: config/skills/M03-skill-a/SKILL.md
      exists: false
  installed:
    paths:
    - path: config/skills/M03-skill-a/SKILL.md
      exists: true
  restored:
    same_as: baseline
fixture_type: component
variants:
  antigravity:
    payload: payload/harnesses/antigravity
    managed_paths:
    - config/skills/M03-skill-a/SKILL.md
  grok-build:
    payload: payload/harnesses/antigravity
    managed_paths:
    - skills/M03-skill-a/SKILL.md
  pi:
    payload: payload/harnesses/pi
    managed_paths:
    - skills/M03-skill-a/SKILL.md
