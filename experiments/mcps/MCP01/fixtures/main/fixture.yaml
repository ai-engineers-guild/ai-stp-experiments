schema_version: 1
fixture_type: component
objects:
- experiment-mcp01
variants:
  antigravity:
    payload: payload/common
    managed_paths:
    - config/mcp_config.json
    source_subpath: .mcp.json
    authoring_path: config/mcp_config.json
  pi:
    payload: payload/harnesses/pi
    managed_paths:
    - extensions/experiment-mcp01/package.json
    - extensions/experiment-mcp01/index.ts
    - extensions/experiment-mcp01/.mcp.json
    - extensions/experiment-mcp01/mcp_servers/experiment-mcp01.py
  grok-build:
    payload: payload/harnesses/grok-build
    exclude: [.mcp.json]
    managed_paths:
    - config.toml
states:
  baseline:
    paths:
    - path: config/mcp_config.json
      exists: false
  installed:
    paths:
    - path: config/mcp_config.json
      exists: true
  restored:
    same_as: baseline
