schema_version: 1
id: H10
component_type: hook
fixtures:
- main
observe:
  expect:
    id: H10
    event: Stop
    matcher: null
    marker: AI_STP_H10_STOP
    output:
      decision: stop
      reason: AI_STP_H10_STOP
  probe: Trigger the installed input hook safely, then call ai_stp_h10_probe and report
    the exact marker. Do not edit files.
  pi_probe:
    mode: hook
    event: input
    tool: ai_stp_h10_probe
variants:
- antigravity
- pi
- grok-build
