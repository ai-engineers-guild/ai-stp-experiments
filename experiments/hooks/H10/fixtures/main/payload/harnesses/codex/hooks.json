{
  "description": "Deterministic Codex Stop projection for H10.",
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import json; print(json.dumps({'hookSpecificOutput': {'hookEventName': 'Stop', 'permissionDecision': 'stop', 'permissionDecisionReason': 'AI_STP_H10_STOP'}}))\"",
            "commandWindows": "py -3 -c \"import json; print(json.dumps({'hookSpecificOutput': {'hookEventName': 'Stop', 'permissionDecision': 'stop', 'permissionDecisionReason': 'AI_STP_H10_STOP'}}))\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
