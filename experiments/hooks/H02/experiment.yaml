schema_version: 1
id: H02
component_type: hook
fixtures:
- main
observe:
  expect:
    id: H02
    event: PreToolUse
    matcher: list_dir
    marker: AI_STP_H02_PRETOOLUSE
    output:
      decision: allow
      reason: AI_STP_H02_ALLOW
  probe: Trigger the installed tool_call hook safely, then call ai_stp_h02_probe and
    report the exact marker. Do not edit files.
  pi_probe:
    mode: hook
    event: tool_call
    tool: ai_stp_h02_probe
variants:
- antigravity
- pi
- grok-build
