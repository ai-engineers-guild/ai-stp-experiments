schema_version: 1
id: H06
component_type: hook
fixtures:
- main
observe:
  codex_probe:
    probe: "Run a harmless directory listing through Codex's Bash tool exactly once, then report the hook decision reason. Do not edit files."
  expect:
    id: H06
    event: PreInvocation
    matcher: null
    marker: AI_STP_H06_PREINVOCATION
    output:
      injectSteps:
      - userMessage: Reply with AI_STP_H06_PRE
  probe: Trigger the installed before_agent_start hook safely, then call ai_stp_h06_probe
    and report the exact marker. Do not edit files.
  pi_probe:
    mode: hook
    event: before_agent_start
    tool: ai_stp_h06_probe
variants:
- antigravity
- pi
- grok-build
- codex
