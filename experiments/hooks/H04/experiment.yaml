schema_version: 1
id: H04
component_type: hook
fixtures:
- main
observe:
  codex_probe:
    probe: "Run a harmless directory listing through Codex's Bash tool exactly once, then report the hook decision reason. Do not edit files."
  expect:
    id: H04
    event: PostToolUse
    matcher: list_dir
    marker: AI_STP_H04_POSTTOOLUSE
    output: {}
  probe: Trigger the installed tool_result hook safely, then call ai_stp_h04_probe
    and report the exact marker. Do not edit files.
  pi_probe:
    mode: hook
    event: tool_result
    tool: ai_stp_h04_probe
variants:
- antigravity
- pi
- grok-build
- codex
