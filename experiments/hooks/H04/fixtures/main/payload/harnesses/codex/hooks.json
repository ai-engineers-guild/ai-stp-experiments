{
  "description": "Deterministic Codex PostToolUse projection for H04.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import json; print(json.dumps({'hookSpecificOutput': {'hookEventName': 'PostToolUse', 'additionalContext': 'AI_STP_H04_POSTTOOLUSE'}}))\"",
            "commandWindows": "py -3 -c \"import json; print(json.dumps({'hookSpecificOutput': {'hookEventName': 'PostToolUse', 'additionalContext': 'AI_STP_H04_POSTTOOLUSE'}}))\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
