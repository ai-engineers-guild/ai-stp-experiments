schema_version: 1
objects:
- PostInvocation:*
states:
  baseline:
    paths:
    - path: config/hooks.json
      exists: false
    - path: config/hooks/h08.py
      exists: false
  installed:
    paths:
    - path: config/hooks.json
      exists: true
    - path: config/hooks/h08.py
      exists: true
  restored:
    same_as: baseline
fixture_type: component
variants:
  antigravity:
    payload: payload/harnesses/antigravity
    managed_paths:
    - config/hooks.json
    - config/hooks/h08.py
  grok-build:
    payload: payload/harnesses/grok-build
    managed_paths:
    - hooks/hooks.json
  pi:
    payload: payload/harnesses/pi
    managed_paths:
    - extensions/experiment-h08.ts
  codex:
    payload: payload/harnesses/codex
    exclude:
    - config
    managed_paths:
    - hooks.json
    source_subpath: hooks.json
    authoring_path: .codex/hooks.json
