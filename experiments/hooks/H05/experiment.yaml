schema_version: 1
id: H05
component_type: hook
fixtures:
- main
observe:
  expect:
    id: H05
    event: PreInvocation
    matcher: null
    marker: AI_STP_H05_PREINVOCATION
    output:
      injectSteps:
      - ephemeralMessage: AI_STP_H05_PRE
  probe: Trigger the installed before_agent_start hook safely, then call ai_stp_h05_probe
    and report the exact marker. Do not edit files.
  pi_probe:
    mode: hook
    event: before_agent_start
    tool: ai_stp_h05_probe
variants:
- antigravity
- pi
- grok-build
