schema_version: 1
id: H09
component_type: hook
fixtures:
- main
observe:
  codex_probe:
    probe: "Run a harmless directory listing through Codex's Bash tool exactly once, then report the hook decision reason. Do not edit files."
  expect:
    id: H09
    event: Stop
    matcher: null
    marker: AI_STP_H09_STOP
    output:
      decision: stop
  probe: Trigger the installed input hook safely, then call ai_stp_h09_probe and report
    the exact marker. Do not edit files.
  pi_probe:
    mode: hook
    event: input
    tool: ai_stp_h09_probe
variants:
- antigravity
- pi
- grok-build
- codex
