schema_version: 1
id: H01
component_type: hook
fixtures:
- main
observe:
  probe: Use run_command to run the harmless dir command, then stop; record the PreToolUse denial reason. Do not edit files.
  expect:
    id: H01
    event: PreToolUse
    matcher: run_command
    marker: AI_STP_H01_PRETOOLUSE
    output:
      decision: deny
      reason: AI_STP_H01_DENY
variants:
- antigravity
