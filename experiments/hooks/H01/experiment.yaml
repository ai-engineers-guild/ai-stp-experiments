schema_version: 1
id: H01
component_type: hook
fixtures:
- main
observe:
  probe: Run the harmless read-only command `Get-ChildItem .` through the normal
    tool interface, then record the blocked result and denial reason. Do not edit
    files.
  pi_probe:
    mode: tool
    tools: [powershell]
    input: Use the powershell tool exactly once with `Get-ChildItem .`, then report
      the exact denial reason.
  codex_probe:
    probe: Run a harmless directory listing through Codex's Bash tool exactly once,
      then report the hook denial reason. Do not edit files.
  expect:
    id: H01
    event: PreToolUse
    matcher: run_command
    marker: AI_STP_H01_PRETOOLUSE
    output:
      decision: deny
      reason: AI_STP_H01_DENY
variants:
- antigravity
- pi
- grok-build
- codex
