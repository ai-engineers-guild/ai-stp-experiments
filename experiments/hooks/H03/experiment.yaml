schema_version: 1
id: H03
component_type: hook
fixtures:
- main
observe:
  codex_probe:
    probe: "Run a harmless directory listing through Codex's Bash tool exactly once, then report the hook decision reason. Do not edit files."
  expect:
    id: H03
    event: PostToolUse
    matcher: run_command
    marker: AI_STP_H03_POSTTOOLUSE
    output: {}
  probe: Trigger the installed tool_result hook safely, then call ai_stp_h03_probe
    and report the exact marker. Do not edit files.
  pi_probe:
    mode: hook
    event: tool_result
    tool: ai_stp_h03_probe
variants:
- antigravity
- pi
- grok-build
- codex
