{
  "description": "Deterministic Codex PostToolUse projection for H03.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import json; print(json.dumps({'hookSpecificOutput': {'hookEventName': 'PostToolUse', 'additionalContext': 'AI_STP_H03_POSTTOOLUSE'}}))\"",
            "commandWindows": "py -3 -c \"import json; print(json.dumps({'hookSpecificOutput': {'hookEventName': 'PostToolUse', 'additionalContext': 'AI_STP_H03_POSTTOOLUSE'}}))\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
