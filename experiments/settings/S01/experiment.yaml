schema_version: 1
id: S01
component_type: setting
fixtures:
- main
observe:
  probe: Start normally with the installed settings and report whether the harness
    accepts them without error; do not inspect files or use tools.
  pi_probe:
    mode: native_settings
    values:
      enableInstallTelemetry: false
  antigravity_probe:
    probe: Start normally with the installed settings. If startup accepts the
      configuration without error, report `enableTelemetry`; do not inspect files
      or use tools.
  expect:
    id: S01
    key: enableInstallTelemetry
    value: false
variants:
- antigravity
- pi
- grok-build
