schema_version: 1
id: S02
component_type: setting
fixtures:
- main
observe:
  codex_probe:
    probe: "What is the installed S02 settings marker? Do not inspect files or use tools."
  expect:
    id: S02
    key: quietStartup
    value: true
  probe: Start Pi normally with the installed settings and report whether they are
    accepted without error; do not edit files or use tools.
  pi_probe:
    mode: native_settings
    values:
      quietStartup: true
variants:
- antigravity
- pi
- grok-build
- codex
