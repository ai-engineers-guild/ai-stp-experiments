schema_version: 1
id: S03
component_type: setting
fixtures:
- main
observe:
  expect:
    id: S03
    key: enableAnalytics
    value: false
  probe: Start Pi normally with the installed settings and report whether they are
    accepted without error; do not edit files or use tools.
  pi_probe:
    mode: native_settings
    values:
      enableAnalytics: false
variants:
- antigravity
- pi
- grok-build
