schema_version: 1
id: S04
component_type: setting
fixtures:
- main
observe:
  codex_probe:
    probe: "What is the installed S04 settings marker? Do not inspect files or use tools."
  expect:
    id: S04
    key: tuiMode
    value: regular
  probe: Start Pi normally with the installed settings and report whether they are
    accepted without error; do not edit files or use tools.
  pi_probe:
    mode: native_settings
    values:
      tuiMode: regular
variants:
- antigravity
- pi
- grok-build
- codex
