schema_version: 2
id: E17
title: Голый + один скилл herdr
prompt: Use herdr only if you must. Say whether HERDR_ENV is required. Do not start
  Herdr.
expect: На голом ящике Agy умеет воспользоваться только herdr. Если apply без провайдера
  падает — verdict=blocked.
expected_verdict: blocked
diagnostic:
  reason: the original named skill fixture is unavailable
  expected_error: AI_STP_NEEDS_CONFIGURATION
phases:
  prepare:
  - id: doctor
    argv:
    - ${ai_stp}
    - doctor
    - --json
  - id: discover
    argv:
    - ${ai_stp}
    - component
    - discover
    - --json
  install: []
  verify: []
